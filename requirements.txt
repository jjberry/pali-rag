# Phase 2 (embedding + index) and Phase 3 (generation).
# The extract/chunk stages and `cli.py check` use only the stdlib.
sentence-transformers==5.6.0
chromadb==1.5.9
anthropic==0.113.0

# Web UI (cli.py web). Already present transitively via the above; pinned here
# so the browser UI can't break if an upstream drops them.
markdown-it-py==4.2.0
Jinja2==3.1.6
